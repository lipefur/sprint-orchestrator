# Addon: hono

## Quando ativar

Backend usa framework Hono (Cloudflare Workers, Bun, Deno, Node).

## Dependências

Nenhuma.

## Detecção automática

- Dependência `hono` em `package.json`
- Imports `from 'hono'` em arquivos `.ts`
