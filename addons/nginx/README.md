# Addon: nginx

## Quando ativar

Frontend ou reverse proxy servido via nginx. Configs em `nginx.conf` ou `nginx/*.conf`.

## Dependências

Nenhuma.

## Detecção automática

- Arquivo `nginx.conf`, `nginx/*.conf`, `default.conf`
- Service `nginx` em `docker-compose.yml`
